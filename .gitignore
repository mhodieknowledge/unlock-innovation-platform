# Secrets — this repository is public (see SECURITY.md §6, FREE_INFRASTRUCTURE.md §3.8).
# No secret may ever be committed. Secrets live in GitHub Actions secrets and
# Cloudflare Worker secrets only.
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
.dev.vars

# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/
.open-next/
.wrangler/
.astro/
*.tsbuildinfo

# Testing / coverage
coverage/
*.lcov

# Supabase local
supabase/.branches/
supabase/.temp/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Byte-budget reports (generated)
.budget-report.json

# The seeded design canvas: the artboards in design/ plus ~2.5 MB of the canvas
# editor baked in. A build output, regenerable in one command from the sources
# beside it — see design/README.md.
design/mbele-redesign.html
