-- Reverses nothing, and says so.
--
-- The organisations this removed were invented from other listings' titles; restoring them
-- would mean re-inventing them, and the opportunities that pointed at them are now correctly
-- attributed to nobody. `deleted_at` is set rather than the rows being dropped, so an operator
-- who disagrees can clear it by hand for a specific row — which is a decision, not a rollback.
SELECT 1;
